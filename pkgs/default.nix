# When you add custom packages, list them here
# NOTE: This is not the packages we're installing. this is to add additional
#       custom packages for nixpkgs so that we can install them
{ pkgs }: {
  # custom_firefox = pkgs.callPackage ./custom_firefox { };
}
